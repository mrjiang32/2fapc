"use strict";
import crypto from "crypto";

import totp from "./totp.js";
import jsenv from "../utils/jsenv.js";

let config = {
  keys: [],
  password_md5: "",
};

function auth(passwd) {

}
